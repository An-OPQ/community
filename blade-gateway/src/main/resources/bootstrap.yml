server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
    loadbalancer:
      retry:
        enabled: true

# 聚合文档配置
blade:
  document:
    resources:
      - name: EasyPoi模块
        location: /blade-easypoi
