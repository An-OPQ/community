<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.hniu.community.dao.FileUploadDao">
    <update id="updateIcon" parameterType="edu.hniu.community.vo.FileUploadVo">
        update userinfo set icon=#{icon} where email=#{email}
    </update>
</mapper>